"""LLM Skill Factory — domain-agnostic core package.

This package contains all the logic for generating, validating, storing and
exporting ``SKILL.md`` files. It deliberately contains **no** Streamlit imports
so it can be unit-tested and reused outside the UI.
"""

__version__ = "0.4.0"
